import { createStore } from 'vuex';

const store = createStore({
    state: () => {
        return {
            name: 'bcl'
        };
    },
    mutations: {},
    actions: {},
    getters: {}
});

export default store;
